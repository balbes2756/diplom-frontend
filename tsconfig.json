{
    "files": [],
    "references": [
        { "path": "./tsconfig.app.json" },
        { "path": "./tsconfig.node.json" }
    ],
    "compilerOptions": {
        "noUnusedLocals": false, // ← отключить проверку неиспользуемых переменных
        "noUnusedParameters": false, // ← отключить проверку неиспользуемых параметров
        "strict": false // ← отключить строгую проверку (НЕ РЕКОМЕНДУЮ)
    }
}
